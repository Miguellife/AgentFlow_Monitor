# AgentFlow Monitor 项目结构

> 本文档描述 AgentFlow Monitor 主项目的目录结构与核心模块。根目录下的演示/子项目不在本文档范围内。

## 项目简介

AgentFlow Monitor 是一个基于 Electron 的桌面悬浮窗应用，用于实时监控多 Provider（DeepSeek / Codex / Kimi / OpenCode）的 API 用量与订阅额度。渲染层采用 React + Vite 重写，主进程数据层为 Provider 适配器架构，统一接口 + capability 驱动调度。

核心特性：

- 多 Provider 用量监控：DeepSeek 余额/用量、Codex/Kimi/OpenCode 订阅制额度
- 五条采集通道：`webSession` / `officialApi` / `accountQuota` / `localLog` / `proxy`，按 provider 声明式启用
- 订阅制额度面板：5h/周窗口进度条 + 重置倒计时
- GitHub 风格 Token 活动热力图（每日/每周/累计三模式）
- 透明置顶悬浮窗：窗口缩放/圆角/状态栏行为与原版一致
- 加密存储 API Key，支持自启动、托盘菜单、设置面板

## 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Electron | ^40.0.0 (开发依赖) / 28 (运行时基线) | 跨平台桌面应用框架 |
| React / React DOM | ^18.3.1 | 渲染层 UI |
| Vite | ^5.4.0 | 渲染层构建（产物 `renderer/dist`） |
| ECharts | ^5.5.0 | 数据可视化图表（本地打包，弃 CDN） |
| gridstack | ^12.3.3 | 网格化布局 |
| electron-store | ^8.1.0 | 持久化设置存储 |
| electron-builder | ^24.9.0 | 应用打包 |
| Node.js | >= 18 (推荐 20+) | 运行环境 |

## 目录结构树

```
agentflow-monitor/
├── src/main/                    # Electron 主进程
│   ├── index.js                 # 应用入口：生命周期、窗口、托盘、调度器接线
│   ├── ipc.js                   # 全部 IPC 处理器 + 缩放状态机
│   ├── store.js                 # 加密持久化存储 + 旧键迁移(providers 命名空间)
│   ├── core/                    # 主进程共享核心
│   │   ├── scheduler.js         # 多 Provider 调度器(capability 驱动轮询)
│   │   ├── http.js              # HTTP 客户端(httpGet/httpPostJson + CONNECT 隧道)
│   │   ├── locallog.js          # 增量文件扫描 + rollupDaily 按日聚合
│   │   └── heatmap.js           # 热力图数据 API(buildHeatmap 纯函数)
│   ├── providers/               # Provider 适配器层
│   │   ├── types.js             # 领域模型:UsageRecord / QuotaState / ProviderAdapter
│   │   ├── registry.js          # 适配器注册表
│   │   ├── deepseek/            # DeepSeek(行为不变迁移)
│   │   │   ├── index.js         #   适配器(webSession/webUsage/balance/realtimeProxy)
│   │   │   ├── usage.js         #   平台用量抓取 + 解析(UsageFetcher)
│   │   │   ├── balance.js       #   余额查询
│   │   │   ├── session.js       #   会话窗口 token 嗅探
│   │   │   └── proxy.js         #   本地代理服务器(拦截实时用量)
│   │   ├── codex/               # Codex(accountQuota + localLog 通道)
│   │   │   ├── index.js         #   适配器
│   │   │   ├── auth.js          #   ~/.codex/auth.json 只读 + OAuth refresh
│   │   │   ├── quota.js         #   wham/usage 额度归一化(经 HTTP 代理)
│   │   │   └── locallog.js      #   rollout-*.jsonl 解析 + 增量扫描
│   │   └── kimi/                # Kimi(accountQuota + localLog 通道)
│   │       ├── index.js         #   适配器
│   │       ├── auth.js          #   ~/.kimi-code/credentials 只读(无刷新端点→降级)
│   │       ├── quota.js         #   /coding/v1/usages 额度归一化
│   │       └── locallog.js      #   wire.jsonl 解析 + 增量扫描
│   │   └── opencode/            # OpenCode(accountQuota + localLog 通道)
│   │       ├── index.js         #   适配器
│   │       ├── auth.js          #   ~/.local/share/opencode/auth.json 只读(Go/Zen 两种)
│   │       ├── quota.js         #   opencode.ai/zen/go/v1/usage 额度归一化
│   │       └── locallog.js      #   opencode.db session 表聚合 + 增量覆盖
│   ├── preload/
│   │   └── preload.js           # 预加载脚本（IPC 白名单桥接）
│   └── renderer/                # 旧渲染层(仅保留设置/登录窗口所需资源)
│       ├── login.html / js/login.js
│       ├── settings-window.html / js/settings-window.js / js/settings-definitions.js
│       ├── js/layout/component-registry.js
│       ├── css/main.css
│       └── assets/
├── renderer/                    # React + Vite 渲染层(主悬浮窗)
│   ├── package.json / vite.config.js / index.html
│   └── src/
│       ├── main.jsx             # React 入口
│       ├── App.jsx              # 壳:ResizeHandles/TitleBar/Dashboard/StatusBar + ctrl+wheel 缩放
│       ├── api.js               # window.api 封装(getProviders/getDashboard/getHeatmap...)
│       ├── store.js             # useSyncExternalStore:providers 快照 + dashboard 缓存
│       ├── styles.css           # 全部样式(含 is-window-resizing 缩放覆盖规则)
│       ├── hooks/
│       │   ├── useProviders.js  # providers/dashboard 订阅 hooks
│       │   └── useECharts.js    # ECharts 生命周期 + ResizeObserver 自适应
│       ├── lib/
│       │   ├── chartTheme.js    # ECharts 主题(getTheme/getBarTheme)
│       │   ├── format.js        # 纯函数:formatReset 倒计时 / formatTokenCount
│       │   └── heatmap.js       # 纯函数:buildWeeks/colorLevel/formatToken/isoWeekKey
│       ├── grid/
│       │   ├── components.js    # 组件注册表数据(与旧 component-registry 同源)
│       │   └── policy.js        # 布局策略纯函数(从旧 layout-policy 转 ESM,逻辑零改动)
│       └── components/
│           ├── ResizeHandles.jsx # 8 手柄缩放(逐行等价旧 app.js:window:set-bounds/is-window-resizing)
│           ├── TitleBar.jsx      # 刷新/设置/布局编辑/最小化/关闭
│           ├── StatusBar.jsx     # 状态点 + 刷新时间
│           ├── Dashboard.jsx     # gridstack 容器 + 额度区 + 热力图槽
│           ├── FeeCard.jsx       # 余额/今日消耗/缓存命中率卡片
│           ├── ChartWidget.jsx   # model-bar 堆叠柱 / token-line / cost-line 曲线
│           ├── QuotaCard.jsx / WindowBar.jsx  # 订阅制额度面板
│           └── TokenHeatmap.jsx  # GitHub 风格活动热力图
├── test/                        # 单元测试(node --test)
├── docs/superpowers/            # 设计规格与实现计划文档
├── assets/                      # 应用级静态资源
├── scripts/                     # 一次性调研/自检脚本(spike-refresh / verify-locallog / fetch-fixtures)
├── package.json                 # 项目依赖与脚本
├── electron-builder.yml         # 打包配置
└── 使用说明.md                    # 用户使用说明
```

## 核心模块详解

### `src/main/` — 主进程

主进程负责窗口管理、数据采集、代理服务与持久化。所有 Node.js 权限敏感操作均在此完成。

| 文件 | 职责 |
|------|------|
| `index.js` | 应用入口。创建并管理主悬浮窗、登录窗、会话窗、设置窗、系统托盘；接线 scheduler 与 IPC；处理单实例锁、窗口边界持久化 |
| `ipc.js` | 全部 IPC 处理器（`get:dashboard` / `get:providers` / `get:heatmap` / 设置 / 缩放状态机） |
| `core/scheduler.js` | 多 Provider 调度器。每 provider 按 capability 独立定时器：usage 10s、quota 60s、balance 60s、localLog 60s；401 → `authStatus='expired'` → 广播 |
| `core/http.js` | HTTP 客户端。`httpGet`/`httpPostJson`，支持 HTTP CONNECT 代理隧道（chatgpt.com 需经 `providers.proxyUrl`） |
| `core/locallog.js` | 增量文件扫描（游标 offset/mtime，截断回退）+ `rollupDaily` 按日聚合（纯函数） |
| `core/heatmap.js` | `buildHeatmap`：按 provider/年聚合每日 token 总量 |
| `providers/` | 适配器层，统一 `ProviderAdapter` 接口（`authStatus` / `fetchBalance` / `fetchUsage` / `fetchQuota` / `readLocalLog`） |
| `store.js` | electron-store 加密存储；旧键 `sessionToken`/`apiKey` → `providers.deepseek.*` 一次性迁移 |

### `src/preload/preload.js` — 预加载脚本

通过 `contextBridge.exposeInMainWorld('api', ...)` 向渲染进程暴露受控的 IPC 接口，所有通道均经白名单校验（含 `get:providers`、`get:heatmap`、`providers:changed` 等）。

### `renderer/` — React 渲染层

主悬浮窗由 Vite 构建为 `renderer/dist`，主进程 `loadRenderer` 直接加载。组件按 capability 驱动：

- `Dashboard.jsx` — gridstack 布局容器，`gridChildren` 用 `useMemo` 冻结避免与 gridstack DOM 冲突；`change` 事件经 `validateLayout` 持久化；编辑模式 `setStatic`；顶部渲染 QuotaCard 额度区，底部渲染 TokenHeatmap
- `ResizeHandles.jsx` — 8 手柄缩放，行为逐行等价旧版：`window:set-bounds` 即时提交、`is-window-resizing` 实心直角渲染、松手后恢复圆角
- `ChartWidget.jsx` — ECharts 本地打包，密度自适应（紧凑卡片模式 / 常规模式）
- `QuotaCard.jsx` — 订阅制额度，windows 数组驱动，`authStatus==='expired'` 时替换为"立即重试"入口（凭证由本机 CLI 维护，应用只读不代授权）
- `TokenHeatmap.jsx` — 每日 53×7 / 每周 ISO 周求和 / 累计高度条三模式 + 月份标签 + tooltip

### `src/renderer/` — 遗留渲染资源

仅保留设置窗口（`settings-window.html`）、登录窗口（`login.html`）与其依赖的脚本/样式；主窗口旧渲染层已删除，由 `renderer/` 替代。

## 测试目录 `test/`

基于 Node.js 内置 `node --test` 运行器。覆盖：provider 适配器（deepseek/codex/kimi/opencode）、调度器、存储迁移、localLog 通道、热力图纯函数、布局策略（policy 断言未改）、React 静态契约（resize 行为 / quota 卡片 / heatmap cells）等。

运行方式：`npm test`

## 构建与配置

### `package.json`

- **入口**：`src/main/index.js`
- **脚本**：
  - `npm start` — 启动 Electron 应用（需先 `npm run build:renderer`）
  - `npm test` — 运行测试
  - `npm run build:renderer` — 构建 React 渲染层到 `renderer/dist`
  - `npm run build:win` — 构建 Windows 安装包（自动链 `build:renderer`）
  - `npm run build:mac` — 构建 macOS 安装包

### `electron-builder.yml`

- **打包文件**：`src/**/*`、`renderer/dist/**/*`、`node_modules/**/*`、`package.json`
- **Windows**：NSIS 安装器，图标 `assets/icon.ico`
- **macOS**：DMG，图标 `assets/icon.icns`

## 架构与数据流

```
┌──────────────────────────────────────────────────────────────┐
│                        主进程 (src/main)                      │
│                                                              │
│  index.js ── 生命周期/窗口/托盘 ──► ipc.js ──► renderer/dist  │
│     │                                                        │
│     ├── core/scheduler.js (capability 驱动轮询)               │
│     │     ├── deepseek ── balance / usage / proxy / session  │
│     │     ├── codex    ── quota(accountQuota) / localLog     │
│     │     ├── kimi     ── quota(accountQuota) / localLog     │
│     │     └── opencode ── quota(accountQuota) / localLog     │
│     │                                                        │
│     ├── core/locallog.js ◄── codex/kimi/opencode 增量         │
│     │     └── store 'usageDaily' ──► heatmap API              │
│     │                                                        │
│     ├── core/http.js ── CONNECT 代理(chatgpt.com) / 直连       │
│     │                                                        │
│     └── store.js ──► userData/.key (AES 加密, providers.*)    │
│                                                              │
└──────────────────────────────┬───────────────────────────────┘
                               │ IPC (白名单通道, token 不越界)
                               │
┌──────────────────────────────┴───────────────────────────────┐
│                   React 渲染层 (renderer/src)                  │
│                                                              │
│  App.jsx ── ResizeHandles/TitleBar/Dashboard/StatusBar        │
│     │                                                        │
│     ├── Dashboard.jsx ── gridstack(policy.js 布局)             │
│     │     ├── FeeCard / ChartWidget (echarts 本地打包)         │
│     │     ├── QuotaCard / WindowBar (额度 + 倒计时)            │
│     │     └── TokenHeatmap (每日/每周/累计)                    │
│     │                                                        │
│     └── store.js ── useSyncExternalStore (providers 快照)      │
└──────────────────────────────────────────────────────────────┘
```

**数据流向**：

1. 调度器按 provider 的 capability 轮询：DeepSeek 拉余额/用量（webSession + officialApi）；Codex/Kimi/OpenCode 拉订阅额度（accountQuota，经 HTTP 代理）
2. localLog 通道增量扫描本机 codex/kimi/opencode 日志，按日聚合写入 store `usageDaily`，供热力图 API 消费
3. 任何 provider 数据/状态更新 → 广播 `providers:changed` 全量快照 → React store 订阅刷新
4. 渲染层 Dashboard 按 gridstack 布局渲染组件；QuotaCard 展示额度窗口与重置倒计时；TokenHeatmap 按 provider/年绘制活动热力图
