# 加密配置排障

## 这个检测项检查什么

检查加密配置文件是否可用；复制或恢复时必须同时复制 `.key` 和 `config.json`。

## 常见失败原因

密钥文件与配置文件不匹配，或配置恢复不完整。

## 安全检查步骤

手动替换前使用恢复备份，并保持 `.key` 与 `config.json` 成对处理。

## 高风险操作提醒

绝不要编辑、重新生成或发布 `.key`。

## 提交 Issue 时附上什么

仅附上复制出的 Diagnostics report，以及失败检测项的 id 和 error code。
